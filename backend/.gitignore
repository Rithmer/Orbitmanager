# Shared workspace rules live in the repository root .gitignore.

# === PostgreSQL backups (keep .gitkeep) ===
backups/*
!backups/.gitkeep

# === ML models (large binary files) ===
models/*.joblib
models/*.onnx
